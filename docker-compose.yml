version: "3.9"
services:
  app:
   build:
     context: .
     dockerfile: dockerfile
     ports:
      - 5000: 5000
     environment: 
      - ASPNETCORE_URLS http://*:5000
     volumes: 
      - ./app:/app
    